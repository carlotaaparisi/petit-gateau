<% include ../partials/header %>

<div class="container">
    <div class="row">
        <div class="col-md-3">
            <p class="lead">Cardiage</p>
            <div class="list-group">
                <li class="list-group-item active">Info 1</li>
                <li class="list-group-item">Info 2</li>
                <li class="list-group-item">Info 3</li>
            </div>
        </div>
        <div class="col-md-9">
            <div class="thumbnail">
                <img class="img-responsive" src="<%= project.image %>">
                <div class="caption-full">
                    <h4 class="pull-right">from 5€</h4>
                    <h4><a><%=project.name%></a></h4>
                    <p><%=project.description%></p>
                    <p><%=project.author.username %></p>
                    
                <!--check if the user "owns" the buttons:-->
                    <% if(currentUser && project.author.id.equals(currentUser._id)){ %>
                    <a class="btn btn-xs btn-warning" href="/work/<%= project._id %>/edit">Edit</a>
                    <form id="delete-form" action="/work/<%= project._id %>/?_method=DELETE" method="POST">
                        <button class="btn btn-xs btn-danger">Delete</button>
                    </form>
                    <% } %>
                    
                </div>
            </div>
        </div>
    </div>
</div>


<% include ../partials/footer %>